// Generated by dts-bundle-generator v9.5.1

/**
 * A function for transforming the value of a reactive.
 */
export type ReactiveFormatter<T, O> = (current_value: T) => O;
/**
 * A function for listening for changes to a reactive.
 */
export type ReactiveSubscription<T> = (old_value: T, new_value: T) => void;
/**
 * A value that may or may not be wrapped in a Reactive
 */
export type MaybeReactive<T> = T | Reactive<T>;
export declare class Reactive<T> {
	#private;
	constructor(default_value: T, formatter?: ReactiveFormatter<any, T> | null);
	/**
	 * Gets the current value of the reactive
	 * @returns the current value of this reactive, possibly processed through a formatter.
	 */
	get value(): T;
	/**
	 * Sets the value and calls handlers.
	 * @param new_value the new value to set this reactive to.
	 */
	set value(new_value: T);
	/**
	 * Subscribe to updates to this reactive.
	 * @param handler Called immediately on subscription, then called whenever the value is changed.
	 */
	subscribe(handler: ReactiveSubscription<T>): void;
	/**
	 * Calls the function provided, then calls all subscriptions.
	 *
	 * Intended for modifying the value using methods that don't return the new value, such as `Array.push()`.
	 * @param updater A callback that may mutate the value.
	 */
	update(updater: (self: this) => void): void;
	/**
	 * Creates a new reactive that follows the value of this one, but has it's value modified by a function.
	 * @param formatter A function that formats the value of the reactive
	 * @returns A new reactive that follows changes to this one, but returns values formatted by the provided function.
	 */
	as<O>(formatter: ReactiveFormatter<T, O>): Reactive<O>;
}
/**
 * Gets a random item from an Array.
 * @typeparam T The type the array holds.
 * @param array The array to get random items from.
 * @returns A random item from the provided array.
 */
export declare function get_random_item<T>(array: Array<T>): T;
/**
 * Gets a random number in a range.
 * @param min The minimum number to generate.
 * @param max The maximum number to generate.
 * @returns A random number between min and max.
 */
export declare function get_random_between(min: number, max: number): number;
/**
 * Gets a random string.
 * @param length How long the string should be.
 * @returns A random alphanumeric string.
 */
export declare function get_random_string(length: number): string;
/**
 * Creates a new {@link Reactive} if the provided one isn't already one.
 * @param initial_value The initial value to set it to.
 * @returns A new reactive wrapping the provided value, or the provided value if it is a {@link Reactive}.
 */
export declare let Variable: <T>(initial_value: T) => Reactive<T>;
/**
 * Subscribe to changes to a value that may or may not be a {@link Reactive}.
 * @param maybe_reactive A value that may or may not be a {@link Reactive}.
 * @param handler Called when first subscribing to the value, and if the value is reactive, when it is changed.
 */
export declare let subscribe: <T>(maybe_reactive: MaybeReactive<T>, handler: ReactiveSubscription<T>) => void;
/**
 * Determines whether a value is a {@link Reactive} or not.
 * @param maybe_reactive Something that may or may not be a {@link Reactive}.
 * @returns Whether the argument is a {@link Reactive} or not.
 */
export declare let is_reactive: <T>(maybe_reactive: MaybeReactive<T>) => maybe_reactive is Reactive<T>;
/**
 * A chainable builder class for DOM elements.
 */
export declare class ElementBuilder {
	_domEl: HTMLElement;
	constructor(type?: string);
	/**
	 * Sets the text content of this element.
	 * @param content The content to be set.
	 * @returns The same elementBuilder it was called on.
	 */
	text(content: MaybeReactive<string>): this;
	/**
	 * Inserts another elementBuilder as a child of this element.
	 * @param element The elementBuilder to insert.
	 * @returns The same elementBuilder it was called on.
	 */
	insert(element: MaybeReactive<ElementBuilder>): this;
	/**
	 * Adds an event listener to this element.
	 * @param type The event to listen for.
	 * @param handler The function to handle the event.
	 * @returns The same elementBuilder it was called on.
	 */
	handle(type: string, handler: MaybeReactive<(type: string, self: ElementBuilder, event: Event) => void>): this;
	/**
	 * Sets a css style property on this element.
	 * @param property The css property to set.
	 * @param value The value to set it to.
	 * @returns The same elementBuilder it was called on.
	 */
	style(property: string, value: MaybeReactive<string>): this;
	/**
	 * Sets a css custom property on this element.
	 * @param property The name of the custom property, without the two leading hyphens.
	 * @param value The value to set it to.
	 * @returns The same elementBuilder it was called on.
	 */
	set_cssvar(property: string, value: MaybeReactive<string>): this;
	/**
	 * Gets the value of a css custom property on this element.
	 * @param property The name of the custom property, without the two leading hyphens.
	 * @returns The value of the custom property.
	 */
	get_cssvar(property: string): string;
	/**
	 * Sets a html attribute on this element.
	 * @param property The property to set.
	 * @param value The value to set it to.
	 * @returns The same elementBuilder it was called on.
	 */
	set_prop(property: string, value: MaybeReactive<string>): this;
	/**
	 * Gets the value of a html attribute on this element.
	 * @param property The property of who's value to get.
	 * @returns The value of the specified property.
	 */
	get_prop(property: string): string;
	/**
	 * Links up a reactive to the value of a property on this element.
	 * @param property The property to get and set the value of
	 * @param variable The reactive that should be linked to the property
	 * @param event An event that will be fired when the property is changed
	 * @returns The same elementBuilder it was called on.
	 */
	bind_prop(property: string, variable: Reactive<string>, event: string): this;
	/**
	 * Subscribe to an arbitrary reactive value but get this element as an argument to the callback as well.
	 * @param variable The reactive value to subscribe to.
	 * @param callback The callback to subscribe with.
	 * @returns The same elementBuilder it was called on.
	 */
	sub<T>(variable: Reactive<T>, callback: (new_value: T, old_value: T, self: ElementBuilder) => void): this;
	/**
	 * Sets the id of this element.
	 * @param id The id to set on the element.
	 * @returns The same elementBuilder it was called on.
	 */
	id(id: string): ElementBuilder;
	/**
	 * Adds a class to this element.
	 * @param name The classname to add.
	 * @returns The same elementBuilder it was called on.
	 */
	add_class(name: MaybeReactive<string>): ElementBuilder;
	/**
	 * Removes a class from this element.
	 * @param name The name of the class to remove.
	 * @returns The same elementBuilder it was called on.
	 */
	remove_class(name: string): ElementBuilder;
	/**
	 * Replaces a class on this element.
	 * @param oldClass The class to be replaced.
	 * @param newClass The class to replace with.
	 * @returns {ElementBuilder} The same elementBuilder it was called on.
	 */
	replace_class(oldClass: string, newClass: string): ElementBuilder;
	/**
	 * Toggles a class on this element.
	 * @param name The class to be toggled
	 * @returns The same elementBuilder it was called on.
	 */
	toggle_class(name: string): ElementBuilder;
	/**
	 * Runs different code based on whether a class exists on this element.
	 * @param name The class to check.
	 * @param yescb Code to run if the class is present.
	 * @param nocb Code to run if the class is not present.
	 * @returns The same elementBuilder it was called on.
	 */
	if_class(name: string, yescb: () => void, nocb: () => void): ElementBuilder;
	/**
	 * Completely clears the contents of this element.
	 * @returns The same elementBuilder it was called on.
	 */
	clear(): ElementBuilder;
	/**
	 * Loops over an array and adds children to this element.
	 * @param list The array to iterate over.
	 * @param itemcallback A callback to call for each item, which returns an elementBuilder to be inserted.
	 * @param blankcallback A callback to call in the case of the array having 0 items, which returns an elementBuilder to be inserted.
	 * @returns The same elementBuilder it was called on.
	 */
	loop<T>(list: MaybeReactive<Array<T>>, itemcallback: (item: T, index: number) => ElementBuilder, blankcallback: () => ElementBuilder, some_wrapper?: (() => ElementBuilder) | false): ElementBuilder;
	/**
	 * Checks whether this element has a class.
	 * @param name The class to check for the presence of.
	 * @returns Whether this element has that class
	 */
	has_class(name: string): boolean;
	/**
	 * Gets the list of all classes on this element.
	 * @returns All the classes on this element.
	 */
	classes(): Array<string>;
	/**
	 * Gets a copy of this element.
	 * @returns A copy of this element
	 */
	get(): ElementBuilder;
	/**
	 * Attaches this elementBuilder to another dom element.
	 * @param element The DOM element to attach to.
	 * @returns The same elementBuilder it was called on.
	 */
	from_dom(element: HTMLElement): ElementBuilder;
	/**
	 * Gets the underlying DOM element of this elementBuilder.
	 * @returns The underlying DOM element of this elementBuilder.
	 */
	to_dom(): HTMLElement;
	/**
	 * Calls the handler function with this element if the provided condition is true.
	 * @param condition The condition to check for.
	 * @param handler The function to run if the condition is true.
	 * @returns The same elementBuilder it was called on.
	 * @deprecated for now because I'm not sure if it's necessary. Will either remove it or undeprecate it.
	 */
	if(condition: boolean, handler: (self: ElementBuilder) => void): this;
	/**
	 * Calls a function with this element as an argument.
	 * @param handler A function to do something with this element.
	 * @returns The same elementBuilder it was called on.
	 */
	process(handler: (self: ElementBuilder) => void): this;
}
/**
 * Selects an element as an {@link ElementBuilder}.
 * @param selector The css selector for an element.
 * @returns An elementBuilder attached to the dom element associated with the provided selector.
 */
export declare function $(selector: string): ElementBuilder;
/**
 * Selects multiple elements as an array of {@link ElementBuilder}s.
 * @param selector The css selector for a collection of elements.
 * @returns An array containing whose are attached to the DOM elements associated with the provided selector.
 */
export declare function $all(selector: string): Array<ElementBuilder>;
/**
 * Injects css into the current webpage.
 * @param css The css to be injected.
 * @deprecated Use a css file for large blocks of style or {@link ElementBuilder.style} for inline styles.
 */
export declare function $css(css: string): void;
/**
 * Creates a new dom element.
 * @param type The type of element to create. Defaults to a div
 * @returns An elementBuilder attached to a brand-new dom element of the specified type.
 */
export declare function _(type?: string): ElementBuilder;

export {};
